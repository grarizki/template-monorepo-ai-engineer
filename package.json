{
  "name": "ai-template-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev": "moon run :dev",
    "build": "moon run :build",
    "lint": "moon run :lint",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.5",
    "@moonrepo/cli": "^2.2.3",
    "husky": "^9.1.7"
  }
}
